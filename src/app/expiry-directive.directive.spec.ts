import { ExpiryDirectiveDirective } from './expiry-directive.directive';

describe('ExpiryDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpiryDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
