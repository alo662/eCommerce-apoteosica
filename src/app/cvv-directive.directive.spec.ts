import { CvvDirectiveDirective } from './cvv-directive.directive';

describe('CvvDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CvvDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
